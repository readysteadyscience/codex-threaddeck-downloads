# SBOM

No SBOM artifact is included in this local draft.

The v0.2.0 project kit package is a static installer package containing CTD project files and a shell installer. The plugin package is an experimental skeleton.

Future public releases can add SPDX or CycloneDX artifacts here after the package inventory format is finalized.
